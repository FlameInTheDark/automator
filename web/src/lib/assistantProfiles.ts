import type { AssistantProfileScope } from '../types'

export const ASSISTANT_SCOPE_LABELS: Record<AssistantProfileScope, string> = {
  pipeline_editor: 'Node Editor',
  chat_window: 'Chat Window',
}
